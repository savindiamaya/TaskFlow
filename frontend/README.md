# TaskFlow Frontend

Next.js (App Router) UI for TaskFlow. See the root [README.md](../README.md) for full setup, environment variables, and deployment instructions.

## Quick start

```bash
cp .env.example .env.local
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
